# Frontend Environment Variables for Vercel

# API Configuration - deployed Laravel API endpoint (the ONLY required var)
# VITE_API_URL is the sole env the frontend reads for the API origin.
VITE_API_URL=https://api.theartainment.co.ke/api
