# Frontend Environment Variables for Production
#
# VITE_API_URL is the ONLY variable the frontend reads for the API origin.
# Set this in Vercel → Project Settings → Environment Variables (Production).
# Vite inlines it at build time; a dashboard value takes precedence over this file.
VITE_API_URL=https://api.theartainment.co.ke/api
